# RNA-TR-Scout Stage 15Q v0.1.1 preflight — Pro audit and v0.1.2 amendment

## Decision

**Preflight evidence: PASS.  
v0.1.1 execute: HOLD / SUPERSEDED.  
v0.1.2 read-only preflight: AUTHORIZED.**

The uploaded v0.1.1 bundle is structurally sound and its 102-file candidate plan is
internally consistent. The hold is caused by acceptance-suite and machine-snapshot
completeness issues found before any project mutation.

## Required corrections

1. Accept the actual completed-shard validator/hash rejection class and prove no new attempt.
2. Check second-resume unit-attempt count as well as output fingerprints.
3. Execute a real biology-sidecar manifest smoke bound to Core manifest SHA and stable IDs.
4. Check the cross-platform boundary from its machine-readable TSV.
5. Include the active caller and package-validator transitive dependency closure.
6. Canonicalize Stage15L/M/N and this Stage15Q amendment audit history.

Only v0.1.2 read-only preflight is authorized next.
