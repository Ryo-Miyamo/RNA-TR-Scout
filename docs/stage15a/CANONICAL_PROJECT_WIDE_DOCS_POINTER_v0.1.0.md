# Canonical project-wide documentation pointer v0.1.0

This Stage-local directory is retained as historical context.

Authoritative project-wide artifacts are under `docs/architecture/`, `docs/governance/`,
`docs/contracts/`, `docs/core_freeze/v0.1.0/`, `validation/golden/v0.1.0/`, and
`metadata/core_freeze/v0.1.0/`.

Origin context: Architecture audit introduced in Stage15A.

Do not delete historical Stage-local documents merely because a canonical copy exists.
